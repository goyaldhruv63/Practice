#include<stdio.h>
int main()
{
	float l,b,area;
	printf("enter the value of l:");
	scanf("%f",&l);
	printf("enter the value of b:");
	scanf("%f",&b);
	area=l*b;
	printf("area of rectangle=%.2f",area);
	return 0;
	
}
